---
name: <skill-name>
description: <一句话说明这个 skill 解决什么问题、何时触发。写具体，用于未来匹配。>
---

# <Skill 名称>

> 一句话定位：`<这个 skill 让 AI 具备什么能力>`

---

## 触发时机（When to use）

- `<什么情况下该用这个 skill>`
- `<什么情况下不该用，避免误触发>`

---

## 输入（Input）

- `<执行前需要准备什么：需求 / 文件 / 上下文>`

---

## 流程（Steps）

> 控制在 10 步以内。超过说明任务该拆。

1. `<第一步做什么>`
2. `<第二步做什么>`
3. `<...>`

---

## 产出（Output）

- `<这个 skill 跑完应该交出什么：文档 / 代码 / 清单>`

---

## 卡点信号（Stop signals）

遇到以下信号，停下来要澄清，不要硬猜：

- `<信号 1，例：需求里出现「大概」「可能」→ PRD 不够清晰>`
- `<信号 2，例：触碰文件 > 10 → 该拆 PR>`

---

## 自检清单（Self-check）

完成后对照检查：

- [ ] `<检查项 1>`
- [ ] `<检查项 2>`
- [ ] 没有越出声明的边界
- [ ] 没有顺手重构无关代码
